import React from 'react';
import { styled } from 'styled-components';

const Home = () => {
  return <>Home</>;
};

const Img = styled.img.attrs({
  src: '/icons/hotdog_removed.png',
})`
  width: 500px;
`;

export default Home;
